boolean = require('./boolean')
make = require('./make')

either = eitherize: (f) -> make.template(f, make.either)
either[k] = either.eitherize(v) for k, v of boolean

module.exports = Object.freeze(either)
