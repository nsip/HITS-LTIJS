boolean = require('./boolean')
make = require('./make')

validation = validationize: (f) -> make.template(f, make.validation)
validation[k] = validation.validationize(v) for k, v of boolean

module.exports = Object.freeze(validation)
