boolean = require('./boolean')
make = require('./make')

option = optionize: (f) -> make.template(f, make.option)
option[k] = option.optionize(v) for k, v of boolean

module.exports = Object.freeze(option)
